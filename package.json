{
  "name": "smyfs-100-plan",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Interactive 100-mile ultramarathon training plan for the Show Me Your Free State race.",
  "scripts": {
    "dev": "npm run ics && vite",
    "ics": "node scripts/generate-ics.mjs",
    "sync": "node scripts/sync-strava.mjs",
    "sync:mock": "node scripts/sync-strava.mjs --mock",
    "prebuild": "npm run ics",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
